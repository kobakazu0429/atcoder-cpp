#include <bits/stdc++.h>
#define len(x) ((int)(x).size())

using namespace std;

int main() {
  cin.tie(0);
  ios::sync_with_stdio(false);

  int X;
  cin >> X;
  if (X >= 30)
    cout << "Yes" << endl;
  else
    cout << "No" << endl;

  return 0;
}
